angular.module('GPApp')
  .run(['$rootScope', function ($rootScope) {
      // Other constants
      //$rootScope.isIPhoneDevice = isIPhone(),
      //$rootScope.isWindowsDevice = isWindowsPhone()
   }]);