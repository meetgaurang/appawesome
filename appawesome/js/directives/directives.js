angular.module('GPApp.directives').directive('gpAppSampleDirective',
    function () {
        return {
            link: function (scope, element, attrs) {
                // Initialize Popup menu
               
            }
        };
    });